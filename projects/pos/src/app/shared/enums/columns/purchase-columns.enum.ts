export enum PurchaseColumnLabel {
  ID = 'Id',
  QUANTITY = 'Quantity',
  SUB_TOTAL = 'Sub Total',
  TAX = 'Tax',
  TOTAL = 'Total',
  STATUS = 'Status',
}

export enum PurchaseColumn {
  ID = 'id',
  QUANTITY = 'quantity',
  SUB_TOTAL = 'sub_total',
  TAX = 'tax',
  TOTAL = 'total',
  STATUS = 'status',
}
