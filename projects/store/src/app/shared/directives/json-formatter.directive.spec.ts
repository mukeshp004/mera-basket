import { JsonFormatterDirective } from './json-formatter.directive';

describe('JsonFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new JsonFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
